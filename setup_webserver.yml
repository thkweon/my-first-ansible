# ==========================================
# 1. Vault SSH CA (인증서 발급 부분은 기존과 동일)
# ==========================================
- name: Get Dynamic SSH Cert from Vault
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Generate temporary SSH keypair
      community.crypto.openssh_keypair:
        path: /tmp/dynamic_ssh_key
        type: ed25519
      register: temp_keypair

    - name: Login to Vault AppRole
      uri:
        url: "{{ lookup('env', 'VAULT_ADDR') }}/v1/auth/approle-ssh/login"
        method: POST
        body_format: json
        body:
          role_id: "{{ lookup('env', 'VAULT_ROLE_ID') }}"
          secret_id: "{{ lookup('env', 'VAULT_SECRET_ID') }}"
      register: vault_login

    - name: Sign SSH Public Key via Vault
      uri:
        url: "{{ lookup('env', 'VAULT_ADDR') }}/v1/ssh/sign/shadowlinux"
        method: POST
        headers:
          X-Vault-Token: "{{ vault_login.json.auth.client_token }}"
        body_format: json
        body:
          public_key: "{{ temp_keypair.public_key }}"
      register: vault_signed_cert

    - name: Save signed certificate to file
      copy:
        content: "{{ vault_signed_cert.json.data.signed_key }}"
        dest: "/tmp/dynamic_ssh_key-cert.pub"
        mode: '0644'

    - name: Apply SSH Key and Certificate to target hosts
      set_fact:
        ansible_ssh_private_key_file: "/tmp/dynamic_ssh_key"
        ansible_ssh_common_args: "-o CertificateFile=/tmp/dynamic_ssh_key-cert.pub -o StrictHostKeyChecking=no"
      delegate_to: "{{ item }}"
      delegate_facts: true
      loop: "{{ groups['all'] }}"

# ==========================================
# 2. 타겟 서버 설치 및 설정 (OS 판별)
# ==========================================
- name: Setup Nginx Web Server
  hosts: all
  # 리눅스는 root 권한(sudo)이 필요하고, 맥(brew)은 보통 현재 사용자 권한으로 실행합니다.
  become: "{{ (ansible_os_family == 'Debian') | ternary(true, false) }}"
  gather_facts: true
  tasks:
    
    # [Linux] Nginx 설치 (Debian/Ubuntu)
    - name: (Linux) Install Nginx using apt
      apt:
        name: nginx
        state: present
        update_cache: yes
      when: ansible_os_family == "Debian"

    # [macOS] Nginx 설치 (Homebrew)
    - name: (macOS) Install Nginx using brew
      community.general.homebrew:
        name: nginx
        state: present
      when: ansible_os_family == "Darwin"

    # [공통] OS별 경로 및 사용자 설정 변수 등록
    - name: Set OS specific variables
      set_fact:
        nginx_conf_dest: "{{ (ansible_os_family == 'Darwin') | ternary('/usr/local/etc/nginx/servers/default.conf', '/etc/nginx/sites-available/default') }}"
        web_root: "{{ (ansible_os_family == 'Darwin') | ternary('/usr/local/var/www', '/var/www/html') }}"

    # [macOS] 설정 폴더가 없을 경우 생성 (brew 전용)
    - name: (macOS) Ensure servers directory exists
      file:
        path: /usr/local/etc/nginx/servers
        state: directory
      when: ansible_os_family == "Darwin"

    # [공통] 9999 포트 설정 파일 배포
    - name: Configure Nginx to listen on 9999
      copy:
        dest: "{{ nginx_conf_dest }}"
        content: |
          server {
              listen 9999;
              server_name localhost;
              root {{ web_root }};
              index index.html;
              location / {
                  try_files $uri $uri/ =404;
              }
          }
      notify: Reload Nginx

    # [공통] index.html 파일 복사
    - name: Copy index.html to web root
      copy:
        src: ./index.html
        dest: "{{ web_root }}/index.html"
        mode: '0644'

  handlers:
    - name: Reload Nginx
      service:
        name: nginx
        state: reloaded
      # 맥에서 서비스 관리자(launchctl)가 앤서블 service 모듈과 연동되지 않을 경우를 대비
      ignore_errors: "{{ ansible_os_family == 'Darwin' }}"

# ==========================================
# 3. Cleanup
# ==========================================
- name: Cleanup temporary key files
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Remove temp keys
      file:
        path: "{{ item }}"
        state: absent
      loop:
        - "/tmp/dynamic_ssh_key"
        - "/tmp/dynamic_ssh_key.pub"
        - "/tmp/dynamic_ssh_key-cert.pub"